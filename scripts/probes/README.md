# Manual probes

These scripts are ad-hoc diagnostics for Betfair HTML parsing and the public
SQLite export. They are intentionally not named `test_*.py` and are not part of
the automated test suite.

Run them from the repository root, for example:

```powershell
.\.venv\Scripts\python.exe scripts\probes\betfair_parse_fixture_events.py
```
